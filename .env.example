# Copy to .env and fill in. NEVER commit the real .env (it is gitignored).

# --- Supabase ---
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# --- Primary trading engine (self-hosted FastAPI on the VPS) ---
# Use a STABLE production URL (custom domain or ngrok reserved domain).
# A free *.ngrok-free.dev tunnel is ephemeral and will break in production.
VITE_API_URL=""
